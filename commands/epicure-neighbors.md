---
name: epicure-neighbors
description: Find nearest Epicure flavour-space neighbours for an ingredient
---

Use Epicure MCP `neighbors` for the ingredient named by the user. Summarise the
closest neighbours and when useful compare one pair with `compare_on_axis` on a
relevant axis. Keep the answer short and actionable; no medical or allergen
claims.
